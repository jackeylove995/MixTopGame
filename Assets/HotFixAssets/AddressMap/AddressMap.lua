--生成全局代码映射，不用手写地址
--映射规则：
--key : 统一为 文件名_后缀，整个项目不要使用同名资源
--value : lua文件为路径用.分割，用以require，预制体路径用/分割，用来Addressables.LoadAsset
--生成全局代码映射，不用手写地址
--映射规则：
--key : 统一为 文件名_后缀，整个项目不要使用同名资源
--value : lua文件为路径用.分割，用以require，预制体路径用/分割，用来Addressables.LoadAsset
--生成全局代码映射，不用手写地址
--映射规则：
--key : 统一为 文件名_后缀，整个项目不要使用同名资源
--value : lua文件为路径用.分割，用以require，预制体路径用/分割，用来Addressables.LoadAsset
--生成全局代码映射，不用手写地址
--映射规则：
--key : 统一为 文件名_后缀，整个项目不要使用同名资源
--value : lua文件为路径用.分割，用以require，预制体路径用/分割，用来Addressables.LoadAsset
--生成全局代码映射，不用手写地址
--映射规则：
--key : 统一为 文件名_后缀，整个项目不要使用同名资源
--value : lua文件为路径用.分割，用以require，预制体路径用/分割，用来Addressables.LoadAsset
--生成全局代码映射，不用手写地址
--映射规则：
--key : 统一为 文件名_后缀，整个项目不要使用同名资源
--value : lua文件为路径用.分割，用以require，预制体路径用/分割，用来Addressables.LoadAsset
--生成全局代码映射，不用手写地址
--映射规则：
--key : 统一为 文件名_后缀，整个项目不要使用同名资源
--value : lua文件为路径用.分割，用以require，预制体路径用/分割，用来Addressables.LoadAsset
Resources = "Resources"
EditorSceneList = "EditorSceneList"
GamePanel_lua = "Assets.HotFixAssets.Game.Lua.GamePanel"
GamePanel_prefab = "Game/Prefabs/GamePanel.prefab"
image_jpg = "Login/Art/image.jpg"
LoginPanel_lua = "Assets.HotFixAssets.Login.Lua.LoginPanel"
login_bg_jpg = "Login/Art/login_bg.jpg"
LoginPanel_prefab = "Login/Prefabs/LoginPanel.prefab"
bg3_jpg = "Login/Art/bg3.jpg"
AddressMap_lua = "Assets.HotFixAssets.AddressMap.AddressMap"
init_lua = "Assets.HotFixAssets.AAAInit.Lua.init"
LevelSetting_lua = "Assets.HotFixAssets.LevelSetting.LevelSetting"
BasePanel_prefab = "AABase/Prefabs/BasePanel.prefab"
AutoMarkAddress_cs = "Assets/Scripts/EditorTool/Editor/AutoMarkAddress.cs"
AddressMap_lua = "Assets.HotFixAssets.AddressMap.AddressMap"
AddressMap_lua = "Assets.HotFixAssets.AddressMap.AddressMap"
AddressMap_lua = "Assets.HotFixAssets.AddressMap.AddressMap"
